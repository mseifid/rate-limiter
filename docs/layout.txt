ratelimiter/
│
├── cmd/
│   └── main.go ← starts HTTP API if you build it as a service   
│
├── api/
│   └── handler.go            ← defines HTTP handlers ()
│
├── configs/
│   └── dev.json     ← environment vars
│
├── pkg/
│   └── ratelimiter/
│        ├── limiter.go       ← interface + factory
│        ├── bucket.go  ← algorithm implementation
│        ├── result.go        ← results & types
|        ├── storage.go    ← storage functionality
|        ├── config.go    ← ratelimiter specific config
|
|   └── utility/  ← app utility functions
│
├── internal/
│   ├── config/
│        └── config.go       ← config exposer to app
│
│
└── go.mod